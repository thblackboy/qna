var questionActionsBlock = $('.question#<%= @question.id %>').children('.actions');
questionActionsBlock.children("a.subscribe-question-link").remove()
questionActionsBlock.append('<%= link_to 'Unsubscribe', unsubscribe_question_question_subscribers_path(@question), method: :delete, class: 'unsubscribe-question-link', remote: true %>');
