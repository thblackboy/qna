<% if question.errors.present? %>
  $('#<%= question.id %>.question-errors').html('<%= render 'shared/errors', resource: question %>');
<% else %>
  $('.question#<%= question.id %>').html('<%= j render question %>');
<% end %>
