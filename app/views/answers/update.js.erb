<% if answer.errors.present? %>
  $('#<%= answer.id %>.answer-errors').html('<%= render 'shared/errors', resource: answer %>');
<% else %>
  $('.answer#<%= answer.id %>').html('<%= j render answer %>');
<% end %>
